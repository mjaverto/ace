name: Drift Watcher

on:
  schedule:
    - cron: "0 12 * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  drift:
    name: Drift Watcher (placeholder)
    runs-on: ubuntu-latest

    steps:
      - name: Drift watcher placeholder
        run: echo "drift watcher placeholder"
